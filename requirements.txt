# JinTai AI Agent - Dependencies
# Install: pip install -r requirements.txt

# Excel processing (read/write BOM files)
openpyxl>=3.1.0

# Data validation (Pydantic models)
pydantic>=2.0.0

# Web backend (FastAPI server)
fastapi>=0.100.0
uvicorn>=0.20.0
python-dotenv>=1.0.0

# LLM integration (OpenRouter, Google AI)
litellm>=1.0.0
